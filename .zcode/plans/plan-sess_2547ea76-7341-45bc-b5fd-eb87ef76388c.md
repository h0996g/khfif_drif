Verify the ride-detail implementation with automated checks (the project has `flutter_test` available but no test directory yet).

### 1. Unit tests — new `test/driver_ride_detail_models_test.dart`
Covers the riskiest layer, JSON parsing, with fixtures matching the swagger `DriverRideDetailResponse` schema:
- **Full payload**: all 21 fields present → every field maps correctly (state string → `RideOutcome.completed`, serviceType → enum, coords to double, timestamps, distance/duration).
- **Partial payload**: only required-ish fields, `finalFare` sent as a JSON double (e.g. `1250.0`), missing coords/distance/duration/cancellation → tolerant defaults (`0` fare only if truly absent, `''` strings, null numerics), no throw.
- **`displayDate` getter**: prefers `completedAt`, falls back to `cancelledAt` then `acceptedAt`.

### 2. Formatter test — new `test/date_formatter_test.dart`
- `formatRideDateTime('2026-08-22T14:05:00Z')` → correct `dd/MM/yyyy · HH:mm` shape (local time), empty string for garbage input; `formatRideDate` sanity check.

### 3. Run `flutter test`
Compiles and executes the new code paths — a stronger check than `flutter analyze` (which is already clean).

### 4. Backend probe (if the server is up)
`curl http://localhost:8080/api/driver/rides/<fake-uuid>`:
- **401** → route exists, auth gate in front (expected without a token) — confirms the deployed backend matches the spec.
- **404** → route not deployed on this backend build.
- Connection refused → backend not running; skip and note it.
(No authenticated end-to-end call is possible from here — driver login requires phone/OTP.)

Report the results of each check when done.